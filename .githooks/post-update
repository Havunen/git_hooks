#!/usr/bin/env bash
set -euo pipefail

hook_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=lib/common.sh
source "$hook_dir/lib/common.sh"

hook_log "server completed ${#} ref update(s)"
for ref_name in "$@"; do
  hook_log "updated-ref=$ref_name"
done
